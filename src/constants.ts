import { StylesNavLink, Urls } from './types';

export const URLS: Urls = {
  services: '/reservaciones/nueva',
  summary: '/sumary'
}

export const STYLES_NAV_LINK: StylesNavLink = {
  isActive: 'bg-blue-600 hover:bg-blue-700',
  notActive: 'bg-gray-200 hover:bg-blue-600 text-blue-600'
}